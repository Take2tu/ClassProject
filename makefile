README.md:
	echo "# Guessing program created for Bash course final name guessinggame.sh" > README.md
		echo "DATE and Time make was run `(date -R)`" >> README.md
			echo  "Lines of code equals `(wc -l guessinggame.sh)`" >> README.md
